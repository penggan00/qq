docker run -d \
  --name qq-translator \
  --restart unless-stopped \
  --memory=512m \
  --cpus=1 \
  -v /docker/qq-translator/data:/app/data \  # 持久化数据目录
  -e TZ=Asia/Shanghai \                      # 显式设置时区
  penggan0/qq-translator:latest